model ProductImage {
  id        String  @id @default(uuid())
  url       String
  alt       String?
  order     Int
  productId String
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)
}